# Bluetooth audio tuning primarily for the Bose QC Ultra headphones.
#
# The headset profile stays registered so the HFP command channel can report
# battery level, while autoswitch is off to keep A2DP as the audio route.

monitor.bluez.properties = {
  bluez5.enable-sbc-xq = true
  bluez5.codecs = [ sbc_xq sbc ]
  bluez5.hfphsp-backend = native
}

wireplumber.settings = {
  bluetooth.autoswitch-to-headset-profile = false
}
